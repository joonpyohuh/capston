# 이 파일을 복사해 .env 로 저장한 뒤 값을 채우세요.

OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# --- Instagram (Meta) Webhook: /api/webhooks/instagram ---
# Meta 개발자 콘솔 → 앱 → Webhooks 구독 URL에
#   https://당신의도메인/api/webhooks/instagram
# 로컬만 쓸 때는 ngrok 등으로 HTTPS 공개 URL 필요
META_VERIFY_TOKEN=
META_APP_SECRET=
